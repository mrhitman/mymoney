mutation addConnector(
  $type: String!
  $description: String!
  $interval: Float!
  $params: JSON!
  $enabled: Boolean!
) {
  addConnector(
    args: {
      type: $type
      description: $description
      interval: $interval
      params: $params
      enabled: $enabled
    }
  )
}
