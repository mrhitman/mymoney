mutation AddWallet($walletCreateData: WalletCreate!) {
  createWallet(walletCreateData: $walletCreateData) {
    id
    name
    type
    description
    pockets {
      amount
      currency {
        id
        name
        symbol
      }
    }
  }
}
