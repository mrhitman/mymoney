query GetStatisticByPeriod($from: Float, $to: Float) {
  statisticByPeriod(from: $from, to: $to) {
    walletId
    wallet {
      id
      name
      description
    }
    pockets {
      amount
      currency {
        id
        name
      }
    }
    createdAt
  }
}
