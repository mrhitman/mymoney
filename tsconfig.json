{
  "extends": "./tsconfig.build.json",

  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "common": ["common"],
      "dashboard": ["dashboard"],
      "api": ["api"],
      "mobile": ["mobile"]
    }
  }
}
